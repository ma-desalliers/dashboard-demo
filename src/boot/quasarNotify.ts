import { boot } from 'quasar/wrappers';
import { Notify } from 'quasar';

export default boot(({ app }) => {
  // No need to assign Notify to app properties, use Notify directly in components or stores
});
